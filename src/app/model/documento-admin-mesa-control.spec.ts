import { DocumentoAdminMesaControl } from './documento-admin-mesa-control';

describe('DocumentoAdminMesaControl', () => {
  it('should create an instance', () => {
    expect(new DocumentoAdminMesaControl()).toBeTruthy();
  });
});
